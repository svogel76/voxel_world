[package]
name = "voxel_game"
version = "0.1.0"
edition = "2024"

[dependencies]
bevy = { workspace = true, features = ["free_camera"] }
bevy_voxel_world = { workspace = true }
avian3d = { workspace = true }
world_generator = { path = "../world_generator" }
grass_generator = { path = "../grass_generator" }
glam = { workspace = true }
rand = { workspace = true }

[dev-dependencies]
# none — unit tests live in the library modules
